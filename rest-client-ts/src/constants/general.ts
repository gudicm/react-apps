import { StringKeyObject } from '../types/index';

export const statusOkRange: StringKeyObject = {
  MIN: 200,
  MAX: 299,
};
export const httpMethods: Array<string> = ['GET', 'POST', 'DELETE', 'PUT', 'HEAD', 'OPTIONS'];
